num proc factorial(num x)
[
	if(x<0)
	{
		out 0;
	};
	if(x&0)
	{
		out 1;
	}
	else
	{
		create num temp;
		temp = x - 1;
		out factorial(temp)*x;
	};
];
num proc sunlight(word a, word b)
[
	create num q;
	q = lexStrCmp(a,b);
	out q;
];

word proc artem(num a)
[
	out 'Seni0r QA - Art_K!';
];

word proc retstr(num a)
[
	out artem(2);
];

entry
[
	create word stroka;
	stroka = retstr(1);
	outStream stroka;
	create num len;
	create num result;
	stroka = 'Hello wolrd!';
	len = stringLen(stroka);
	len = stringLen('Õóé')+lexStrCmp('Qwe', '')*factorial(2);
	result = sunlight(stroka, 'Hello wolrd!');
	outStream result;
	outStream factorial(5)*2;
	outStream len;
	out 0;
];
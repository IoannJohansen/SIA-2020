num proc sum(num a, num b)
[
	out a+b;
];
num proc isequals(num a, num b)
[
	if(a&b)
	{
		out 0;
	}
	else
	{
		out 1;
	};
];
num proc getlen(word string)
[
	out stringLen(string);
];
entry
[
	create num rescmp;
	rescmp = isequals(1,2);
	if(rescmp&0)
	{
		outStream 'Числа одинаковы';
	}
	else
	{
		outStream 'Числа различны';
	};

	create num ressum;
	ressum = sum(15,35);
	outStream(ressum);
	outStream getlen('Hay');
	out 0;
];
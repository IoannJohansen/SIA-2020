num proc ex(num x, num y)
[
	create num z;
	create word lee;
	lee = 'Hello world!';
	z = stringLen(lee);
	z=(x+y)*5;
	out z;
];

num proc false(num qwe)
[
	outStream qwe;
	create word hay;
	qwe = qwe+2;
	out qwe;
];

entry
[
	create num koo;
	create word who;
	create word foo;
	create num lee;
	who = 'Hello world!';
	koo = 5;

	if(koo > 4)
	{
		outStream 'Yes';
		koo = 4;
		lee = lexStrCmp(who, foo);
	}
	else
	{
		outStream 'No';
	}
	koo = ex(1,4);
	koo = false(koo);
	lee = stringLen(foo);
	lee = lexStrCmp(who, foo);
	outStream lee;
	outStream koo;
	out koo;
];
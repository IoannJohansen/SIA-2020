num proc ex(num x, num y)
[
	create num z;
	create word lee;
	lee = 'Hello world!';
	z = stringLen(lee);
	z = stringLen(lee);
	z = stringLen(lee);
	z=(x+y)*y;
	out z;
];

word proc false(num qwe)
[
out qwe;
create word hay;
hay = 'heselhoff';
out hay;
];

entry
[
	create num koo;
	create word who;
	create word foo;
	create num lee;
	who = 'Hello world!';
	if(5 < 4)
	{
		outStream 'Yes';
	}
	else
	{
		outStream 'No';
	}
	foo = 'Всем гл';
	koo = ex(1,4);
	lee = stringLen(foo);
	lee = stringLen(foo);
	lee = stringLen(foo);
	lee = lexStrCmp(who, foo);
	outStream lee;
	outStream koo;
];
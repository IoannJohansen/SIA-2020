num proc ex(num x, num y)
[
	create num z;
	create word lee;
	lee = 'Hello world!';
	z = stringLen(lee);
	z=(x+y)/2;
	out z;
];

word proc false(num qwe)
[
	outStream qwe;
	create word hay;
	hay = 'heselhoff';
	out hay;
];

entry
[
	create num koo;
	create word who;
	create word foo;
	create num lee;
	who = 'Hello world!';
	if(koo < 4)
	{
		outStream 'Yes';
		lee = lexStrCmp(who, foo);
	}
	else
	{
		outStream 'No';
	}
	foo = 'Всем гл';
	koo = ex(1,4);
	foo = false(koo);
	lee = stringLen(foo);
	lee = lexStrCmp(who, foo);
	outStream lee;
	outStream koo;
	out koo;
];